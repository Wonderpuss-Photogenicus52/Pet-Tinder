const express = require("express");
// placeholder for controller
const controller = require("./conftrollerFolder");
const router = express.Router();

router.get('/login', );

router.post('/signup', );

router.get('/home', );

// ADD CONTROLLER CONNECTIONS













module.exports = router;